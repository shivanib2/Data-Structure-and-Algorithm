#include <iostream>
#include <vector>

using namespace std;

int main() {

    //first way with vector
   /* // Create a jagged array with 3 rows of different lengths
    vector<vector<int>> jaggedArray;
    
    // First row with 3 elements
    jaggedArray.push_back(vector<int>{1, 2, 3});
    
    // Second row with 2 elements
    jaggedArray.push_back(vector<int>{4, 5});
    
    // Third row with 4 elements
    jaggedArray.push_back(vector<int>{6, 7, 8, 9});

    // Print the jagged array
    for (int i = 0; i < jaggedArray.size(); ++i) {
        for (int j = 0; j < jaggedArray[i].size(); ++j) {
            cout << jaggedArray[i][j] << " ";
        }
        cout << endl;
    }

*/

//second way using reference variable


    return 0;
}